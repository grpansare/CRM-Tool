# Application Configuration
spring.application.name=crm-activity-service
server.port=8084

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=crm_activities
spring.data.mongodb.authentication-database=admin

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=localhost

# RabbitMQ Configuration - DISABLED FOR NOW
# spring.rabbitmq.host=localhost
# spring.rabbitmq.port=5672
# spring.rabbitmq.username=guest
# spring.rabbitmq.password=guest

# Event Configuration - DISABLED FOR NOW
# crm.events.exchange=crm.events
# crm.events.activity.created.routing-key=activity.created

# Logging Configuration
logging.level.com.crmplatform.activity=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Security Configuration
jwt.secret=mySecretKey
jwt.expiration=86400000

# Pagination Configuration
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100
