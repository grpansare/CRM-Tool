# Server Configuration
server.port=8087

# Application Configuration
spring.application.name=crm-email-service

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/crm_email_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@grpansare123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=parkease.ag@gmail.com
spring.mail.password=bwlr lstp dywi agvz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cloud Stream Configuration
spring.cloud.stream.bindings.emailEvents-in-0.destination=email.events
spring.cloud.stream.bindings.emailEvents-in-0.group=email-service
spring.cloud.stream.bindings.emailNotifications-out-0.destination=email.notifications

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Management Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging Configuration
logging.level.com.crmplatform.email=DEBUG
logging.level.org.springframework.mail=DEBUG
